# PATCHED v0.5.10 Dockerfile.codex — Add blank line after header

FROM node:22-slim
ENV CODEX_VERSION=latest \
    NODE_ENV=production \
    CODEX_QUIET_MODE=1
RUN npm install -g @openai/codex@$CODEX_VERSION \
 && useradd -m runner
USER runner
WORKDIR /workspace
ENTRYPOINT ["codex"]

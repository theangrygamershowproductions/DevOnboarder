# PATCHED v0.1.28 docker/Dockerfile.frontend — Vite container

FROM node:22-slim
WORKDIR /app
COPY .. /app
RUN corepack enable && pnpm --dir frontend install --frozen-lockfile
CMD ["pnpm", "--dir", "frontend", "dev"]

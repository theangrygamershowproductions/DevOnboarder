# Framework Phase 2 Configuration

# Script execution settings
FRAMEWORK_ROOT="/home/potato/DevOnboarder/frameworks/friction_prevention"
SCRIPT_TIMEOUT="300"  # 5 minutes default timeout
LOG_LEVEL="INFO"
CENTRALIZED_LOGGING="true"

# Quality gates
QC_THRESHOLD="95"
SHELLCHECK_ENABLED="true"
PYLINT_ENABLED="true"
MARKDOWN_LINT_ENABLED="true"

# Environment settings
VIRTUAL_ENV_REQUIRED="true"
FEATURE_BRANCH_REQUIRED="true"
MAIN_BRANCH_PROTECTION="true"

# Automation settings
AUTO_PR_CLEANUP="true"
AUTO_ISSUE_MANAGEMENT="true"
AUTO_BRANCH_CLEANUP="false"  # Manual confirmation required

# Monitoring settings
METRICS_COLLECTION="true"
PERFORMANCE_MONITORING="true"
ERROR_TRACKING="true"

# Integration settings
CI_INTEGRATION="true"
GITHUB_INTEGRATION="true"
DISCORD_NOTIFICATIONS="false"

# Security settings
TOKEN_VALIDATION="true"
SSH_KEY_VALIDATION="true"
ENV_FILE_PROTECTION="true"

# Path configurations
AUTOMATION_PATH="${FRAMEWORK_ROOT}/automation"
WORKFLOW_PATH="${FRAMEWORK_ROOT}/workflow"
PRODUCTIVITY_PATH="${FRAMEWORK_ROOT}/productivity"
DEVELOPER_EXPERIENCE_PATH="${FRAMEWORK_ROOT}/developer_experience"
DOCS_PATH="${FRAMEWORK_ROOT}/docs"
CONFIG_PATH="${FRAMEWORK_ROOT}/config"

# Log paths (using centralized logging)
LOG_DIR="/home/potato/DevOnboarder/logs"
ERROR_LOG="${LOG_DIR}/framework_errors.log"
EXECUTION_LOG="${LOG_DIR}/framework_execution.log"
METRICS_LOG="${LOG_DIR}/framework_metrics.log"

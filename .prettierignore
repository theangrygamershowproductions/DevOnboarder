# Exclude GitHub workflow files from Prettier formatting
# These files need to follow yamllint rules, not Prettier formatting
.github/workflows/*.yml
.github/workflows/*.yaml

# Exclude other files that have specific linting requirements
*.yml
*.yaml

# Exclude markdown files - they have their own linting rules via markdownlint
*.md

#!/bin/sh
# PATCHED v0.1.58 hooks/pre-commit — enable Redis mock for tests
# Pre-commit hook to lint and test repository
set -e

export REDIS_MOCK=1

black --check .
pnpm --dir frontend lint
pnpm --dir frontend test:unit
pnpm --dir auth-server run test
pytest -q

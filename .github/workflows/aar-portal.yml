name: Generate AAR Portal (GPG Signed)
# Automated AAR portal generation with cryptographic signature verification
# Triggers: AAR file changes, daily schedule, manual dispatch
# Bot: DevOnboarder AAR Bot with GPG signing for commit authenticity

on:
  push:
    paths:
      - '.aar/**'
      - 'scripts/generate_aar_portal.py'
    branches: [main]
  pull_request:
    paths:
      - '.aar/**'
      - 'scripts/generate_aar_portal.py'
    branches: [main]
  schedule:
    # Regenerate daily at 06:00 UTC
    - cron: '0 6 * * *'
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: "aar-portal"
  cancel-in-progress: false

jobs:
  generate-portal:
    runs-on: ubuntu-latest

    steps:
      - name: Set token for AAR Portal authentication
        id: set-token
        run: |
          # Multi-step token selection following Priority Matrix pattern
          if [ -n "${{ secrets.CI_ISSUE_AUTOMATION_TOKEN }}" ]; then
              echo "selected-token=${{ secrets.CI_ISSUE_AUTOMATION_TOKEN }}" >> $GITHUB_OUTPUT
              echo "token-source=CI_ISSUE_AUTOMATION_TOKEN" >> $GITHUB_OUTPUT
          elif [ -n "${{ secrets.CI_BOT_TOKEN }}" ]; then
              echo "selected-token=${{ secrets.CI_BOT_TOKEN }}" >> $GITHUB_OUTPUT
              echo "token-source=CI_BOT_TOKEN" >> $GITHUB_OUTPUT
          elif [ -n "${{ secrets.GITHUB_TOKEN }}" ]; then
              echo "selected-token=${{ secrets.GITHUB_TOKEN }}" >> $GITHUB_OUTPUT
              echo "token-source=GITHUB_TOKEN" >> $GITHUB_OUTPUT
          else
              echo "::error::No authentication token available for AAR Portal operations!"
              exit 1
          fi
          echo "Using token source: $(cat $GITHUB_OUTPUT | grep token-source | cut -d= -f2)"

      - name: Checkout repository
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd  # TAGS-SHA-PIN: actions/checkout@v5.0.1 (2025-11-13)
        with:
          token: ${{ steps.set-token.outputs.selected-token }}
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@83679a892e2d95755f2dac6acb0bfd1e9ac5d548  # TAGS-SHA-PIN: actions/setup-python@v6.1.0 (2025-11-24)
        with:
          python-version: '3.12'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install jinja2

      - name: Setup GPG commit signing (AAR Bot)
        env:
          AAR_BOT_GPG_PRIVATE: ${{ secrets.AAR_BOT_GPG_PRIVATE }}
          AAR_BOT_GPG_KEY_ID: ${{ vars.AAR_BOT_GPG_KEY_ID }}
          AAR_BOT_NAME: ${{ vars.AAR_BOT_NAME }}
          AAR_BOT_EMAIL: ${{ vars.AAR_BOT_EMAIL }}
        run: |
          # Import GPG private key (using printf to avoid terminal output violations)
          printf '%s\n' "$AAR_BOT_GPG_PRIVATE" | base64 -d | gpg --batch --import --quiet

          # Configure git to use GPG signing
          git config --global user.name "$AAR_BOT_NAME"
          git config --global user.email "$AAR_BOT_EMAIL"
          git config --global user.signingkey "$AAR_BOT_GPG_KEY_ID"
          git config --global commit.gpgsign true
          git config --global gpg.format openpgp

          # Set up non-interactive GPG trust for automation
          gpg --batch --no-tty --command-fd 0 --edit-key "$AAR_BOT_GPG_KEY_ID" <<EOF
          trust
          5
          y
          quit
          EOF

          echo "GPG signing configured for AAR Bot automation"

      - name: Generate AAR Portal
        run: |
          python scripts/generate_aar_portal.py \
            --aar-dir .aar \
            --output-dir docs/aar-portal

      - name: Configure git authentication for AAR Portal
        if: github.event_name != 'pull_request' && github.ref == 'refs/heads/main'
        run: |
          # Configure git to use the scoped token for authentication (Priority Matrix pattern)
          git config --local url."https://${{ steps.set-token.outputs.selected-token }}@github.com/".insteadOf "https://github.com/"
      - name: Create Pull Request for portal updates (GPG signed)
        if: github.event_name != 'pull_request' && github.ref == 'refs/heads/main'
        env:
          GH_TOKEN: ${{ steps.set-token.outputs.selected-token }}
          # Token source for debugging
          TOKEN_SOURCE: ${{ steps.set-token.outputs.token-source }}
        run: |
          # Check if there are changes
          if git diff --quiet docs/aar-portal/; then
            echo "No changes to AAR portal"
            exit 0
          fi

          # Create a new branch for the changes
          BRANCH="automated/aar-portal-update-$(date +%Y%m%d-%H%M%S)"
          git checkout -b "$BRANCH"

          # Stage and commit changes with GPG signature
          git add docs/aar-portal/
          git commit -m "DOCS(aar): update AAR portal [signed]

          - Regenerated HTML portal from AAR data
          - Updated trend analysis and metrics
          - AAR Bot automation: cryptographically signed

          Portal Features:
          - Interactive AAR dashboard
          - Trend analysis and metrics
          - Searchable AAR archive
          - Quality metrics tracking

          [skip ci]"

          # Push the branch
          git push origin "$BRANCH"

          # Create pull request with enhanced description
          gh pr create \
            --title "DOCS(aar): automated AAR portal update [GPG signed]" \
            --body "Automated update of AAR portal with latest data - cryptographically signed by AAR Bot.

          **Changes:**
          - Regenerated HTML portal from AAR data
          - Updated trend analysis and metrics
          - GPG-signed commits for authenticity verification

          **Security:**
          - All commits cryptographically signed by DevOnboarder AAR Bot
          - Signature verification available via GitHub commit interface
          - Bot identity: $(git config user.name) <$(git config user.email)>

          **Automation Details:**
          - Generated via AAR Portal workflow
          - Triggered by: ${{ github.event_name }}
          - Branch: $BRANCH
          - Commit verification: GPG signature required

          This PR was automatically created by the AAR Portal workflow with GPG signing." \
            --label "automated,docs,aar-system,gpg-signed" \
            --head "$BRANCH" \
            --base main

      - name: Setup Pages
        if: github.ref == 'refs/heads/main' && github.event_name != 'pull_request'
        uses: actions/configure-pages@983d7736d9b0ae728b81ab479565c72886d7745b  # TAGS-SHA-PIN: actions/configure-pages@v5.0.0 (2024-03-30)

      - name: Upload to GitHub Pages
        if: github.ref == 'refs/heads/main' && github.event_name != 'pull_request'
        uses: actions/upload-pages-artifact@56afc609e74202658d3ffba0e8f6dda462b719fa  # TAGS-SHA-PIN: actions/upload-pages-artifact@v3.0.1 (2024-02-07)
        with:
          path: docs/aar-portal

      - name: Deploy to GitHub Pages
        if: github.ref == 'refs/heads/main' && github.event_name != 'pull_request'
        id: deployment
        uses: actions/deploy-pages@d6db90164ac5ed86f2b6aed7e0febac5b3c0c03e  # TAGS-SHA-PIN: actions/deploy-pages@v4.0.5 (2024-03-18)

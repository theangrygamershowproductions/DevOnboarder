# ---
# title: "OpenAPI Validation Workflow"
# description: "GitHub Actions workflow for validating OpenAPI specifications and generating documentation previews"
# author: "TAGS Engineering"
# created_at: "2025-09-05"
# updated_at: "2025-09-05"
# tags: ["openapi", "validation", "documentation", "swagger", "ci", "workflow"]
# project: "DevOnboarder"
# document_type: "workflow"
# status: "active"
# visibility: "internal"
# codex_scope: "TAGS"
# codex_role: "Engineering"
# codex_type: "WORKFLOW"
# codex_runtime: true
# codex-agent:
#   name: Agent.OpenAPI
#   role: Validates OpenAPI specifications and generates previews
#   scope: .github/workflows/openapi-validate.yml
#   triggers: Push to main/develop/feat/openapi-phase-2 and pull requests
#   output: OpenAPI validation results and Swagger UI previews
#   gpg-signing: Required for commit verification
# ---
name: openapi-validate
permissions:
  contents: read
  actions: write  # Required for artifact upload in preview job
on:
  pull_request:
  push:
    branches: [main, develop, feat/openapi-phase-2]
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd  # TAGS-SHA-PIN: actions/checkout@v5.0.1 (2025-11-13)
      - name: Set up Python
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
        with:
          python-version: '3.12'
      - name: Create virtual environment and install dependencies
        run: |
          python -m venv .venv
          source .venv/bin/activate
          pip install -e .[test]
      - name: Validate OpenAPI Specifications
        run: |
          source .venv/bin/activate
          python -m openapi_spec_validator openapi/devonboarder.ci.yaml

  preview:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd  # TAGS-SHA-PIN: actions/checkout@v5.0.1 (2025-11-13)
      - run: npm i -g @redocly/cli
      - name: Bundle HTML docs
        run: redocly build-docs openapi/devonboarder.ci.yaml -o openapi-preview.html
      - name: Upload artifact
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: openapi-preview
          path: openapi-preview.html

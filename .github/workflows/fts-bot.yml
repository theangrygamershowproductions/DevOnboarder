# Placeholder workflow - disabled pending implementation
name: FTS Bot

permissions:
  contents: read

# Disabled workflow - only triggers on workflow_dispatch for manual testing
on:
  workflow_dispatch:

# Will be implemented in future development phases

jobs:
  placeholder:
    runs-on: ubuntu-latest
    if: false  # Disabled
    steps:
      - name: Placeholder
        run: echo "Workflow disabled pending implementation"

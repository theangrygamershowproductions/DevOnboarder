# =============================================================================
# File: frontend/Dockerfile.dev
# Version: 2.0.0 - Enhanced for DevOnboarder production readiness
# Author: DevOnboarder Project
# Created: 2025-07-25
# Updated: 2025-08-05
# Purpose: React frontend development container with security and reliability
# Dependencies: Node.js 22, Vite
# DevOnboarder Project Standards: Compliant with copilot-instructions.md
# =============================================================================

FROM node:22-slim

# Set working directory
WORKDIR /app

# Install system dependencies
RUN apt-get update && apt-get install -y \
    curl \
    git \
    && rm -rf /var/lib/apt/lists/*

# Copy frontend-specific package files
COPY frontend/package*.json ./

# Install dependencies
RUN npm ci

# Copy frontend source code
COPY frontend/ ./

# Create logs directory (centralized logging policy)
RUN mkdir -p logs

# Create non-root user for security
RUN useradd -m -s /bin/bash frontenduser && \
    chown -R frontenduser:frontenduser /app
USER frontenduser

# Health check for Vite dev server
HEALTHCHECK --interval=30s --timeout=5s --start-period=15s --retries=3 \
    CMD curl -f http://localhost:3000 || exit 1

# Expose frontend development port
EXPOSE 3000

# Development command with Vite dev server
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
